#include <stdio.h>
#include <string.h>

int main()
{

    char string1[] = "Bro";
    char string2[] = "code";

    // strlwr(stringl);              //converts a string to lowercase
    // strupr( stringl);             //converts a string to uppercase
    // strcat(stringl, string2);     //appends string2 to end of stringl
    // strncat(stringl, string2,1); // Broc //*appends n characters from string2 to str
    // strcpy(stringl, string2);     //copy string2 to stringl
    // strncpy(stringl, string2, 1)  //cro //copy n characters of string2 to stringl

    printf("%s, string1");

    // int result = strlen(string1);
    // int result = strcmp(string1, string2);     //string compare all characters if same then return 0 else -1
    // int result = strncmp(string1, string2,1);  //string compare n characters ie nth letter
    // int result = strcmpi(string1, string2);      //string compare all characters if same then return 0 else -1(ignore case sensitive)
    // int result = strnicmp(string1, string1, 1); //string compare n characters ie nth letter (ignore case sensitive)

    printf("%d, result");
}